<%= form_for(@task) do |f| %>

  <%= f.label :title %>
  <%= f.text_field :title %>

  <%= f.label :details %>
  <%= f.text_field :details %>

  <%= f.label "Completed?" unless @task.new_record?%>
  <%= check_box_tag 'task[completed]', true, task[:completed] unless @task.new_record?%>

  <%= f.submit %>

<% end %>
